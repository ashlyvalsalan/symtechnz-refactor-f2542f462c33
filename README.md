#Modifications Made#
1.Included the functions in try-catch block
  The exceptions that can happen in the  code any time, so the use of try -catch block solve this problem by handling this exception by catch block
2.Commented the functions and api calls
  Comment gives more clarity to the code. So other developer or tester can easily understand the purpose of specific functions.
3.Closed the SQL connection.
  Closing the connection is useful to prevent any other objects from reusing the same connection after we have done with it.
Future Changes
--------------
Common functions can be breakdown into small function in such a way it follows single responsibility principle.
